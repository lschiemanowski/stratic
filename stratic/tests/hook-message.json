{
  "id": "hook-message",
  "name": "Normal hooks can inspect the proposal and amend only its commit message",
  "description": "Normal hooks can inspect the proposal and amend only its commit message.",
  "code": [
    {
      "path": "test/review.test.ts",
      "passage": {
        "quote": "test('Normal hooks can inspect the proposal and amend only its commit message', t => {\n  const root = fixture(t), proposal = preparedRelease(root);\n  hook(root, 'pre-commit', 'git diff --cached --name-only | grep src/queue.ts >/dev/null');\n  hook(root, 'commit-msg', 'printf \"\\\\nReviewed queue change\\\\n\" >> \"$1\"');\n  const accepted = accept(root, proposal.id);\n  assert.equal(head(root), accepted.commit);\n  assert.match(git(root, ['log', '-1', '--format=%B']), /Reviewed queue change/);\n});"
      }
    }
  ],
  "verifies": [
    {
      "description": "commit-safeguards",
      "passage": {
        "quote": "Commit hooks run against an index containing the prepared tree. They may inspect the proposal and amend its commit message. A failing pre-commit or message hook stops acceptance, and a hook that changes project content or the prepared tree also stops it before the branch advances. The live proposal and branch are checked again after the hooks."
      }
    }
  ]
}

{
  "id": "source-boundaries",
  "name": "Repository reads cannot escape through paths or symbolic links",
  "description": "Repository reads cannot escape through paths or symbolic links.",
  "code": [
    {
      "path": "test/project.test.ts",
      "passage": {
        "quote": "test('Repository reads cannot escape through paths or symbolic links', t => {\n  const root = fixture(t);\n  assert.throws(() => readSource(root, '../secret'), /Invalid repository path/);\n  assert.throws(() => readSource(root, '.git/config'), /Invalid repository path/);\n  symlinkSync('/etc/passwd', join(root, 'src/outside'));\n  assert.throws(() => readSource(root, 'src/outside'), /Symbolic links/);\n  const tree = snapshot(root, ['src/outside']);\n  assert.throws(() => readSource(root, 'src/outside', tree), /Not a regular file/);\n});"
      }
    }
  ],
  "verifies": [
    {
      "description": "passages",
      "passage": {
        "quote": "Repository reads constrain where those targets can point: paths must stay inside the project and displayed resources must be regular files within the size limit. Working reads reject symbolic links, directories, and special files such as named pipes before reading their contents. Historical reads validate the selected Git entry independently of the current filesystem, so replacing a working file or directory with a symlink does not hide its earlier regular-file version. A link cannot escape those boundaries merely because its quote is valid."
      }
    }
  ]
}
